services:
  launch-system:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/launch.db
    volumes:
      - ./data:/app/data
      # Mount prompts so /amend edits and manual constitution changes persist
      # and take effect without rebuilding the image.
      - ./prompts:/app/prompts
